name: Build RemoteLink APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Install Android API 36
        run: yes | sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Build debug APK
        run: |
          chmod +x tools/prepare-build.sh
          ./tools/prepare-build.sh

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: RemoteLink-v0.3-alpha-debug
          path: RemoteLink-v0.3-alpha-debug.apk
          if-no-files-found: error
          retention-days: 14
